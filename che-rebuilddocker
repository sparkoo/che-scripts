#!/bin/bash

# shellcheck source=env.sh
. "$( dirname "${0}" )/env.sh"

sh "${SCRIPT_DIR}"/che-build assembly

pushd "${CHEDIR}"/dockerfiles/che || exit
sh -x build.sh --organization:quay.io/mvala --prefix:che --name:server --tag:custom --dockerfile:Dockerfile
popd || exit
