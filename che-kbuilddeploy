#!/bin/bash

set -e

# shellcheck source=env.sh
SCRIPT_DIR=$( dirname "${0}" )
. "${SCRIPT_DIR}/env.sh"

pushd "${CHEDIR}"/wsmaster || exit
mvn clean install
popd || exit

sh "${SCRIPT_DIR}"/che-krebuild
sh "${SCRIPT_DIR}"/che-kdeploycustom
sh "${SCRIPT_DIR}"/che-krestartpod
