#!/bin/bash

set -e

# shellcheck source=env.sh
. "$( dirname "${0}" )/env.sh"

IMAGE=quay.io/mvala/che-operator:latest

pushd "${CHEOPERATORDIR}" || exit
docker build -t "${IMAGE}" .
docker push "${IMAGE}"
popd
